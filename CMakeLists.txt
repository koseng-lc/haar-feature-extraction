project(haar)
cmake_minimum_required(VERSION 2.8.1)
add_compile_options(-std=c++11)
find_package(OpenCV REQUIRED)
add_executable(${PROJECT_NAME} haar.cpp)
target_link_libraries(${PROJECT_NAME} ${OpenCV_LIBRARIES})
